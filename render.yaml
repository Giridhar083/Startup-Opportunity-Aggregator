services:
  - type: web
    name: startup-opportunity-aggregator
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
    disk:
      name: appdata
      mountPath: /app/data
      sizeGB: 1
